{
  "name": "alpinia-prospect-pro",
  "version": "2.0.0",
  "scripts": {
    "build": "echo 'static site — no build step'",
    "migrate": "node scripts/migrate-to-firestore.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.6.1",
    "firebase-admin": "^12.7.0"
  }
}
